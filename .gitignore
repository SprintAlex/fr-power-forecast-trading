# Secrets
.env

# Python
.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/

# Data (raw pulls can be re-fetched from ENTSO-E)
data/raw/*.parquet
data/processed/*.parquet
data/raw/*.csv
data/processed/*.csv

# Results / artifacts
results/*.png
results/*.html
results/*.csv
results/models/

# Graphify (knowledge-graph output, rebuild with `graphify update .`)
graphify-out/

# OS
.DS_Store

# Claude working notes (private strategy — keep local, not for the public remote)
CLAUDE.md
